import React, {useState, useEffect} from 'react';
import axios from 'axios';

// function for onClick

export const IndividualPickup = () => {
    return (
        <div>
            <h2>Sprouts Info</h2>
            <button>Accept Pickup</button>
            {/* create a card */}
        </div>
    )
}
